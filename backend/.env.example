# backend 환경변수
# 사용법: cp .env.example .env

NODE_ENV=development
PORT=3000

# PostgreSQL — infra/docker-compose/.env 와 값을 맞춘다
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=queue
DB_PASSWORD=queue_password
DB_NAME=queue_commerce

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
